[project]
name = "patient-triage-env"
version = "1.0.0"
description = "Patient Complaint Triage Environment"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "openai>=1.0",
    "fastapi",
    "uvicorn[standard]",
]